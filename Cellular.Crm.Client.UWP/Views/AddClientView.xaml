<Page
    x:Class="Cellular.CRM.Client.UWP.Views.AddClientView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Cellular.CRM.Client.UWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBox Width="150" PlaceholderText="Id" Text="{x:Bind AddClientViewModel.Id}"/>
            <TextBox Width="150" PlaceholderText="FirstName" Text="{x:Bind AddClientViewModel.FirstName}"/>
            <TextBox Width="150" PlaceholderText="LastName" Text="{x:Bind AddClientViewModel.LastName}"/>
            <TextBox Width="150" PlaceholderText="Password" Text="{x:Bind AddClientViewModel.Password}"/>
            <Button Content="Add Client" Click="{x:Bind AddClientViewModel.AddNewClient}">
            </Button>
        </StackPanel>
        <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Center">
            <TextBlock Width="250" Text="{x:Bind AddClientViewModel.Error,Mode=TwoWay}"/>
        </StackPanel>
    </StackPanel>

</Page>
