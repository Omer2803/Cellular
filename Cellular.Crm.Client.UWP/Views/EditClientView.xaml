<Page
    x:Class="Cellular.CRM.Client.UWP.Views.EditClientView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Cellular.CRM.Client.UWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Orientation="Vertical" DataContext="{x:Bind editClientViewModel.clientEdited,Mode=TwoWay}" >
        <TextBox Width="150" Text="{Binding Id}"  />
        <TextBox Width="150" Text="{Binding FirstName}"/>
        <TextBox Width="150" Text="{Binding LastName}"/>
        <TextBox Width="150" Text="{Binding RegisterationDate}"/>
        <TextBox Width="150" Text="{Binding ClientTypeId}"/>
        <Button Content="Save details" Width="100" Click="{x:Bind editClientViewModel.SaveDetails}"/>
    </StackPanel>
</Page>
