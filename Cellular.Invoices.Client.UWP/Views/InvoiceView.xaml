<Page
    x:Class="Cellular.Invoices.Client.UWP.Views.InvoiceView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Cellular.Invoices.Client.UWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="10">
            <StackPanel Visibility="{x:Bind ViewModel.IsEmployee}">
                <TextBlock Text="Client ID:"/>
                <TextBox Text="{x:Bind ViewModel.ClientId, Mode=TwoWay}"/>
            </StackPanel>
            
            <ComboBox Header="Numbers:" ItemsSource="{x:Bind ViewModel.Numbers}"/>

            <DatePicker Date="{x:Bind ViewModel.From, Mode=TwoWay}" Header="Start Date:"/>
            <DatePicker Date="{x:Bind ViewModel.Until, Mode=TwoWay}" Header="End Date:"/>

            <Button Content="Calculate" HorizontalAlignment="Center" Click="{x:Bind ViewModel.GetInvoice}"/>
        </StackPanel>
    </Grid>
</Page>
